parameters:
    level: 5  # Choose the strictness level (0 is the loosest, 9 is the strictest)
    paths:
        - src

    treatPhpDocTypesAsCertain: false

    bootstrapFiles:
        - vendor/php-stubs/woocommerce-stubs/woocommerce-stubs.php

    excludePaths:
        - src/View/*

    ignoreErrors:
        - '#Constant DB_NAME not found#'
        - '#Action callback returns bool but should not return anything.#'
